# src/can_borrow.py
from typing import Tuple

def can_borrow(
    item_type: str,
    age: int,
    loan_length_days: int,
    fees: bool,
    carpentry_training: str,
    gardening_training: str,
) -> Tuple[bool, int]:
    return (True, -1)

